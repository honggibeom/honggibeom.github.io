---
title: StockAdvisor 뉴스에서 종목 뽑아내기 - Gemini 대신 사전 매칭으로
date: 2026-09-01
category: stockadvisor
src: cover.svg
tags: [python, 정규식, 뉴스, 종목추출, stockadvisor]
summary: 뉴스 기사가 어느 종목 얘기인지 붙여 주던 Gemini 호출을 걷어내고, 종목명 사전과 정규식으로 다시 만들었다. 한국어 조사 경계, 오탐 규칙, 중복 기사 제거까지 규칙 하나하나가 생긴 이유를 기록한다.
---

> StockAdvisor는 Spring Boot + React로 만든 주식 정보 서비스다. 시세와 뉴스, 포트폴리오, 위험 분석을 한 화면에서 보여주고, 데이터 파이프라인은 Python으로 돌린다. 이 글은 그중 **뉴스 기사에 관련 종목을 붙이는 기능**을 다시 만든 기록이다. 같은 기사에 호재/악재 점수를 붙이는 쪽은 [감성 모델 글](#/post/StockAdvisor%20뉴스%20감성%20분류%2C%20한국어%20BERT를%20파인튜닝해서%20Spring에서%20ONNX로%20돌리기)에 따로 정리했다.

## 왜 바꿨나

처음엔 Gemini API 한 번 호출로 두 가지를 같이 처리했다. 기사 제목과 본문을 넣으면 "이 기사는 호재인가 악재인가"와 "어느 종목 얘기인가"를 한 번에 돌려줬다. 편했다. 문제는 세 가지였다.

| 문제 | 증상 |
|---|---|
| 호출 제한 | 검색어당 100건 × 수십 개 검색어 = 하루 수천 건. 중간에 429가 나면 그날 수집은 반쪽 |
| 실시간 불가 | 사용자가 검색할 때마다 LLM 호출은 응답 시간으로도 비용으로도 말이 안 된다 |
| 환각 | 기사에 없는 종목을 붙이거나, "SK오션플랜트"를 엉뚱한 종목 코드로 연결 |

셋 중 환각이 제일 아팠다. 종목 코드는 틀리면 바로 티가 나는 값이다.

그래서 두 문제를 성격에 맞게 갈랐다. 종목 추출은 "기사에 실제로 있는 이름만 잡는" **결정론적 방식**으로, 감성은 따로 학습한 **작은 분류 모델**로. 이 글은 앞쪽 얘기다.

<svg viewBox="0 0 760 120" width="100%" style="max-width:760px;display:block;margin:24px auto" role="img" aria-label="뉴스 파이프라인 흐름">
  <g font-size="13" fill="var(--color-text)" text-anchor="middle">
    <rect x="4" y="30" width="120" height="56" rx="8" fill="var(--color-bg-subtle)" stroke="var(--color-border)"/>
    <text x="64" y="54">검색어 수집</text><text x="64" y="72" fill="var(--color-text-dim)" font-size="11">"종목명 주가"</text>
    <rect x="164" y="30" width="120" height="56" rx="8" fill="var(--color-bg-subtle)" stroke="var(--color-border)"/>
    <text x="224" y="54">중복 제거</text><text x="224" y="72" fill="var(--color-text-dim)" font-size="11">글자 2-gram</text>
    <rect x="324" y="30" width="120" height="56" rx="8" fill="var(--color-accent-soft)" stroke="var(--color-accent)"/>
    <text x="384" y="54" font-weight="600">종목 추출</text><text x="384" y="72" fill="var(--color-text-dim)" font-size="11">사전 매칭 · 이 글</text>
    <rect x="484" y="30" width="120" height="56" rx="8" fill="var(--color-bg-subtle)" stroke="var(--color-border)"/>
    <text x="544" y="54">감성 점수</text><text x="544" y="72" fill="var(--color-text-dim)" font-size="11">BERT · 다른 글</text>
    <rect x="644" y="30" width="112" height="56" rx="8" fill="var(--color-bg-subtle)" stroke="var(--color-border)"/>
    <text x="700" y="62">DB 저장</text>
  </g>
  <g stroke="var(--color-text-dim)" stroke-width="1.5" fill="none">
    <path d="M124 58h34m-6-5l6 5-6 5"/><path d="M284 58h34m-6-5l6 5-6 5"/><path d="M444 58h34m-6-5l6 5-6 5"/><path d="M604 58h34m-6-5l6 5-6 5"/>
  </g>
</svg>

## 1. 기본 아이디어 - 사전과 정규식

DB의 `stock` 테이블에는 국내외 종목의 `name`과 `kor_name`이 있다. 이걸 전부 모아서 하나의 정규식 alternation(`삼성전자|SK하이닉스|현대차|...`)으로 만들고 기사 제목과 본문에서 찾는다. 모델도 외부 호출도 없으니 기사 한 건 처리에 1ms도 안 걸리고, 사전에 없는 종목이 튀어나올 일이 없다.

### 긴 이름부터 정렬해야 하는 이유

alternation을 만들 때 **이름을 길이 내림차순으로 정렬**하는 게 첫 번째 규칙이다. 정규식 엔진은 `A|B`에서 "가장 긴 쪽"이 아니라 **"먼저 적힌 쪽"**을 시도하고, 성공하면 거기서 멈춘다.

| 패턴 순서 | 입력 "삼성전자우 급등" | 결과 |
|---|---|---|
| `삼성전자\|삼성전자우` | `삼성전자`가 먼저 성공 → 멈춤 | 삼성전자 (틀림) |
| `삼성전자우\|삼성전자` | `삼성전자우` 먼저 시도 → 성공 | 삼성전자우 (맞음) |

`LG|LG전자|LG화학`도 같은 문제다. 긴 것부터 적어 두면 엔진이 긴 이름을 먼저 시도하고, 실패했을 때만 짧은 이름으로 내려온다.

### 사전 세 겹

`stock` 테이블 이름만으로는 부족해서 사전을 세 겹 더 얹었다.

| 사전 | 예 | 이유 |
|---|---|---|
| 약칭 `ALIAS_EXTRA` | 아모레, LG생건, 금호석화, 한화에어로, SKT, 모비스, 엔씨, 카뱅, 두산에너빌, KAI | 기사는 정식 명칭을 거의 안 쓴다. "아모레퍼시픽"보다 "아모레"가 훨씬 많다 |
| 1:N 별칭 `MULTI_ALIAS` | 삼전닉스 → 005930 + 000660, 반도체 투톱 → 005930 + 000660, 하닉 → 000660 | 한 단어가 두 종목을 가리키는 관용 표현. 별칭 하나가 코드 여러 개로 펼쳐진다 |
| 미국 종목 한글명 `US_NAME` | 엔비디아 → NVDA, 브로드컴 → AVGO, 홈디포 → HD (약 60개) | 미국 종목은 한국 기사에 한글로만 나온다. `NVIDIA`로는 국내 기사가 안 잡힌다 |

세 겹 다 결국 "문자열 → 종목 코드 목록" 맵이고, 매칭 로직은 하나다. 사전만 갈아 끼우면 된다.

## 2. 한국어라서 생기는 문제 - 조사 경계

여기가 첫 번째로 오래 걸린 부분이다.

### 영어의 `\b`가 한국어에서 안 되는 이유

영어라면 `\b`(단어 경계)로 끝난다. `\bApple\b`은 "Apple"은 잡고 "Applesauce"는 안 잡는다. `\b`는 "단어 문자와 단어가 아닌 문자 사이"를 뜻하는데, 영어는 단어 사이에 항상 공백이 있으니 이게 곧 단어 경계다.

한국어는 **종목명 뒤에 조사가 띄어쓰기 없이 바로 붙는다.** "삼성전자가", "삼성전자를", "삼성전자에서는". 정규식 엔진 입장에서 한글은 전부 단어 문자라서 "삼성전자"와 "가" 사이에 경계가 없다. `\b삼성전자\b`는 "삼성전자가 발표했다"에서 아무것도 못 잡는다.

그렇다고 "뒤에 한글이 와도 무조건 허용"으로 풀면 반대 문제가 터진다. **"하이브"가 "하이브리드"에, "신스틸"이 "신스틸러"에** 걸린다. 자동차 기사마다 하이브(엔터테인먼트)가 붙는 걸 보고 알았다.

### 규칙 - 앞은 막고, 뒤는 조사만 열어 둔다

| 위치 | 글자 | 판정 | 예 |
|---|---|---|---|
| 이름 **앞** | 한글 | ✕ 매칭 안 함 | 한국투자증권 → "투자증권" 부분 매칭 방지 |
| 이름 **앞** | 공백·문장 시작·기호·영문 | ○ | "…, 삼성전자는" |
| 이름 **뒤** | 한글이 아님 | ○ | "삼성전자 3분기", "삼성전자(005930)" |
| 이름 **뒤** | 한글 + `JOSA_HEAD`에 있음 | ○ | 삼성전자**가**, 현대차**그**룹 |
| 이름 **뒤** | 한글 + `JOSA_HEAD`에 없음 | ✕ | 하이브**리**드, 신스틸**러** |

앞쪽은 단순하다. 앞에는 조사가 붙을 일이 없으니 한글이면 무조건 막는다. 뒤쪽이 핵심인데, "그 글자가 조사(또는 조사처럼 붙는 글자)인가"만 본다.

```python
JOSA_HEAD = (
    "가이은는을를에의와과도로으만부까나라서보처등및요야지여며든게대마뿐조밖커토했하한된될인입니"
    + "그측주株계노사"
)
```

앞부분은 조사의 첫 글자들이다. "가/이/은/는/을/를/에/의/와/과/도/로/으(로)/만/부(터)/까(지)/처(럼)/등/및…". 중간의 "했/하/한/된/될/인/입/니"는 조사는 아니지만 "현대차인가", "카카오입니다", "삼성전자하고"처럼 서술어가 바로 붙는 경우를 위한 것이다.

뒤에 따로 붙인 `그측주株계노사`는 **"현대차그룹", "삼성측", "삼성주", "LG계열", "삼성노조", "현대차사"** 같은 표현 때문에 하나씩 늘어난 것이다. 자동으로 만든 목록이 아니다. dry-run을 돌려서 "잡혀야 하는데 안 잡힌 것"을 보면서 한 글자씩 추가했다.

### 정규식으로 옮기면

```python
# (?<![가-힣])   앞 글자가 한글이면 실패 (부정 후방 탐색)
# (?![가-힣])    뒤 글자가 한글이 아니면 통과 (부정 전방 탐색)
# (?=[JOSA])     한글이면 JOSA_HEAD 중 하나여야 통과 (긍정 전방 탐색)
pattern = re.compile(
    r"(?<![가-힣])(" + "|".join(map(re.escape, names_longest_first)) + r")"
    r"(?:(?![가-힣])|(?=[" + JOSA_HEAD + r"]))"
)
```

세 가지 "탐색"은 전부 **글자를 소비하지 않고 조건만 검사**한다. 그래서 조사 자체는 매칭 결과에 포함되지 않고 종목명만 깔끔하게 나온다. `(?<!...)`는 "바로 앞이 이게 아니어야", `(?!...)`는 "바로 뒤가 이게 아니어야", `(?=...)`는 "바로 뒤가 이거여야"다.

이 규칙이 완벽하진 않다. 조사 목록에 있는 글자가 우연히 다른 단어의 첫 글자인 경우는 원리상 통과한다. 그래서 목록에 한 글자 넣을 때마다 dry-run으로 오탐이 늘지 않는지 같이 봤다.

## 3. 오탐을 잡기 위한 규칙들

규칙을 만든 뒤 기존 기사 2,141건에 돌려 보니 빈도 상위에 이상한 종목이 올라왔다. 하나씩 규칙이 됐다.

| 문제 | 규칙 | 효과 |
|---|---|---|
| "나스닥"이 나스닥 거래소(NDAQ)에 32건 | `BLOCK_NAMES` — 코스피, 다우, S&P, 대만 반도체 등 지수·시장 이름 차단 | 지수 기사에 거래소 종목이 붙는 일이 사라짐 |
| "LS증권 김OO 연구원은…" 같은 출처 언급 | `TITLE_ONLY_CODES` + 이름에 "증권"/"금융투자"가 있으면 자동 적용. 제목에 나올 때만 연결 | 언론사·증권사가 자기 기사도 아닌데 붙던 문제 해결 |
| "[헤드라인]", "이 시각 주요뉴스" 같은 모아보기 | 제목이 이 패턴이면 본문을 아예 안 봄 | 기사 하나에 종목 스무 개 붙는 일 방지 |
| 본문에서만 잡힌 종목의 44%가 노이즈 | `TITLE_ONLY_TICKERS=1` — **제목에 나온 종목만 연결** | 아래 참고 |

### 본문 매칭을 끈 근거

결정적인 건 마지막 줄이다. 본문에서만 잡힌 종목을 300건 표본으로 하나씩 확인했더니 이랬다.

<svg viewBox="0 0 640 96" width="100%" style="max-width:640px;display:block;margin:20px auto" role="img" aria-label="본문에서만 잡힌 종목 220건 중 정상 123건, 노이즈 97건">
  <text x="0" y="16" font-size="13" fill="var(--color-text-muted)">본문에서만 잡힌 종목 220건</text>
  <rect x="0" y="30" width="335" height="22" rx="4" fill="var(--color-accent)"/>
  <rect x="337" y="30" width="263" height="22" rx="4" fill="#e5534b"/>
  <text x="8" y="46" font-size="13" fill="#06131a" font-weight="600">정상 123</text>
  <text x="345" y="46" font-size="13" fill="#ffffff" font-weight="600">노이즈 97 (44%)</text>
  <text x="0" y="82" font-size="12" fill="var(--color-text-dim)">노이즈 = 비교 대상("삼성전자와 달리 LG전자는…"), 경쟁사, 출처 언급</text>
</svg>

붙인 것의 44%가 틀린 연결이면 안 붙이는 게 낫다. 그래서 운영은 **제목에 나온 종목만 연결**한다.

이건 정밀도와 재현율의 교환이다. 정밀도는 "붙인 것 중 맞는 비율", 재현율은 "붙였어야 하는 것 중 실제로 붙인 비율"이다. 본문 매칭을 끄면 재현율은 조금 떨어지지만(본문에서만 언급된 종목을 놓친다) 정밀도가 크게 오른다. "이 기사는 이 종목 얘기다"라는 의미를 생각하면 제목에 없는 종목은 원래 그 기사의 주인공이 아니니, 이 교환은 남는 장사다. 본문 매칭 로직은 남겨 두고 플래그로 끈 거라, 나중에 "언급된 종목"이라는 별도 개념이 필요해지면 다시 켤 수 있다.

## 4. 검색어에 "주가" 붙이기

수집 쪽에서 생긴 문제다. 미국 종목을 한글 이름으로 검색하면 엉뚱한 게 온다. **"애플"로 검색하면 과즙세연이 나오고, "테슬라"로 검색하면 헬로카봇이 나온다.** 종목명이 일반 명사거나 다른 고유명사와 겹치는 경우다.

종목 검색어에는 전부 " 주가"를 접미로 붙였다(`TICKER_QUERY_SUFFIX`). "애플 주가"로 검색하면 과즙은 안 온다. 기사를 저장할 때 검색어를 태그로 남기는데, 이때는 접미를 떼서 원래 이름만 남긴다. 태그에 "애플 주가"가 찍히면 보기 흉하다.

## 5. 중복 기사 제거

같은 사건을 열 군데 언론사가 쓰면 제목이 조금씩 다른 기사가 열 건 쌓인다. 현대차 부사장 영입 기사가 **66건** 들어온 걸 보고 중복 제거를 넣었다.

### 어절 자카드는 왜 실패했나

처음엔 제목을 띄어쓰기로 쪼개서(어절 단위) 자카드 유사도를 구했다. 자카드는 `|A ∩ B| / |A ∪ B|`, 두 집합이 겹치는 비율이다. 그런데 **"공급계약"과 "공급 계약"이 다른 토큰**이 된다. 한국어 띄어쓰기는 언론사마다 제각각이라 같은 제목이 다르게 쪼개지고, 유사도가 뚝 떨어진다. 어절 단위는 버렸다.

### 글자 2-gram + 오버랩 계수

**글자 2-gram 집합**으로 바꿨다. 2-gram은 연속한 두 글자 묶음이다. "공급계약"은 `{공급, 급계, 계약}`이 되고, "공급 계약"도 공백을 빼면 같은 집합이다. 띄어쓰기 차이에 강하고, 조사 하나 바뀌는 정도는 2-gram 한두 개 차이로 끝난다.

유사도는 자카드 대신 **오버랩 계수** `|A ∩ B| / min(|A|, |B|)`를 썼다. 분모를 합집합이 아니라 **작은 쪽 집합 크기**로 두는 이유는, 짧은 제목이 긴 제목에 포함되는 경우를 잡기 위해서다.

짧은 제목 A의 2-gram이 8개, 긴 제목 B가 20개, 교집합이 7개라고 하자. A의 내용은 거의 전부 B 안에 들어 있다.

| 지표 | 계산 | 값 | 판정 (임계 0.55) |
|---|---|---|---|
| 자카드 | 7 / (8 + 20 − 7) | 0.33 | 다른 기사 |
| 오버랩 계수 | 7 / min(8, 20) | **0.88** | 같은 기사 |

긴 제목 쪽이 붙인 수식어("글로벌 경쟁력 강화 위해…") 때문에 자카드는 낮게 나오지만, 실제로는 같은 사건이다. 오버랩 계수는 "작은 쪽이 큰 쪽에 얼마나 담겨 있나"를 보기 때문에 이걸 잡는다.

임계값은 **0.55**. 최근 3일치 제목과 비교해서 넘으면 저장하지 않는다. 비교 범위를 두는 이유는, 너무 오래된 기사까지 비교하면 "삼성전자, 신제품 출시"처럼 주기적으로 반복되는 제목이 다른 사건인데도 중복으로 잡히기 때문이다.

### 결과

<svg viewBox="0 0 640 110" width="100%" style="max-width:640px;display:block;margin:20px auto" role="img" aria-label="중복 제거 전 2,445건, 후 1,928건">
  <g font-size="13" fill="var(--color-text)">
    <text x="0" y="28">적용 전</text>
    <rect x="70" y="14" width="500" height="20" rx="4" fill="var(--color-border-strong)"/>
    <text x="578" y="29" fill="var(--color-text-muted)">2,445건</text>
    <text x="0" y="68">적용 후</text>
    <rect x="70" y="54" width="394" height="20" rx="4" fill="var(--color-accent)"/>
    <text x="472" y="69" fill="var(--color-text-muted)">1,928건</text>
  </g>
  <line x1="464" y1="48" x2="464" y2="82" stroke="var(--color-border)" stroke-width="1"/>
  <text x="70" y="100" font-size="12" fill="var(--color-text-dim)">기존 DB에 소급 적용 — 517건(21%)이 같은 사건의 중복 기사였다</text>
</svg>

## 6. 결과

- 셀프테스트 15케이스 통과, 환각 0건. 사전에 있는 이름만 나오니 구조적으로 0건이다
- Gemini 시절 잘못 붙어 있던 티커를 라벨링하면서 여럿 고쳤다. 상한가 종목 4개짜리 기사에 엉뚱한 코드가 붙어 있는 식이었다
- 로직이 전부 규칙이라 Java로 옮기면 실시간 검색 탭에서도 그대로 쓸 수 있다

## 삽질 하나

`pipeline_config.json`에 주석을 쓰고 싶어서 JSONC로 만들었는데, 뉴스 수집 스크립트는 그냥 `json.load`를 쓰고 있었다. 주석이 있으면 파싱 실패 → 예외를 잡고 기본값 → **조용히 기본 검색어 5개만 돌고 있었다.** 검색어 수십 개를 등록했는데 기사가 이상하게 적다 싶으면 설정 파서부터 의심하자. 실패를 조용히 삼키는 기본값 폴백은 편한 만큼 위험하다.

## 남은 것

- 사전 매칭 로직 Java 포팅 → 실시간 검색 탭에 종목 연결 적용
